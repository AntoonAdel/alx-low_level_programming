#include "main.h"

/**
  * mul - function that multiplies two integers.
  * @a: the first integer
  * @b: the second integer
  * Return: Always 0 (Success)
  */

int mul(int a, int b)
{
	int a, b, c;

	c = a * b
	_putchar(c);

	return (0);
}
